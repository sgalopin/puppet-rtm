#!/bin/bash
set -e
set -o pipefail

# Default configuration :
environment=production

while [ "x$1" != "x"  ]
do
  case "$1" in
      "--env" | "-e" )
          shift
          environment=$1
          shift
          ;;
      "--help" | "-h" | * )
          echo "Install Rtm Services"
          echo "  Usage : $(basename $0) [options]"
          echo "    Options :"
          echo "      -e, --env    : Build environment [development, production] (default: « $environment »)"
          echo "      -h, --help   : Print help information and exit"
          exit
          ;;
  esac
done

echo "-- Installation of Rtm Services (env=$environment)"

echo "-- Stopping tomcat"
/etc/init.d/tomcat8 stop

echo "-- Decompression of the jdk 1.6"
cd <%= $git_clone_directory %>/services_configs
rm -rdf jdk1.6.0_45
chmod 700 jdk-6u45-linux-x64.bin && ./jdk-6u45-linux-x64.bin

echo "-- Build of libs_java"
cd <%= $git_clone_directory %>/services_configs/libs_java && ant build

echo "-- Build of service_common"
mkdir -p <%= $git_clone_directory %>/service_common/classes
cd <%= $git_clone_directory %>/services_configs/service_common && ant compile

echo "-- Build of service_generation_rapport"
mkdir -p <%= $git_clone_directory %>/service_generation_rapport/images
cd <%= $git_clone_directory %>/service_generation_rapport && ant deploy

echo "-- Build of service_harmonization"
sed -i "s|log4j.rootLogger=.*|log4j.rootLogger=ERROR, stdout|" <%= $git_clone_directory %>/service_harmonization/config/log4j.properties
sed -i "s|log4j.logger.fr.ifn=.*|log4j.logger.fr.ifn=ERROR, logfile|" <%= $git_clone_directory %>/service_harmonization/config/log4j.properties
sed -i "s|log4j.appender.logfile.File=.*|log4j.appender.logfile.File=/var/log/tomcat8/HarmonizationService.log|" <%= $git_clone_directory %>/service_harmonization/config/log4j.properties
cd <%= $git_clone_directory %>/services_configs/service_harmonization && ant deploy

echo "-- Build of service_integration"
sed -i "s|log4j.rootLogger=.*|log4j.rootLogger=ERROR, stdout|" <%= $git_clone_directory %>/service_integration/config/log4j.properties
sed -i "s|log4j.logger.fr.ifn=.*|log4j.logger.fr.ifn=ERROR, logfile|" <%= $git_clone_directory %>/service_integration/config/log4j.properties
sed -i "s|log4j.appender.logfile.File=.*|log4j.appender.logfile.File=/var/log/tomcat8/IntegrationService.log|" <%= $git_clone_directory %>/service_integration/config/log4j.properties
mkdir -p <%= $git_clone_directory %>/service_integration/classes
cd <%= $git_clone_directory %>/services_configs/service_integration && ant deploy

echo "-- Copy of harmonization service"
# Must be done per the ant deploy but it's not working and the build file can't be updated (revision fixed)
cp <%= $git_clone_directory %>/services_configs/service_harmonization/RTMHarmonizationService.xml \
<%= $tomcat_directory %>/conf/Catalina/localhost/RTMHarmonizationService.xml

echo "-- Starting tomcat"
/etc/init.d/tomcat8 start
